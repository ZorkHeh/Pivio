[Unit]
Description=MPVPaper Animated Wallpaper
After=graphical-session.target
PartOf=graphical-session.target

[Service]
Type=simple
ExecStart={{SERVICE_SCRIPT_PATH}} "{{VIDEO_PATH}}"
Restart=always
RestartSec=1

[Install]
WantedBy=graphical-session.target
